# docs — 正式文档入口

正式文档位于：

产品与架构文档单独维护（不随代码仓库公开）。仓库内的各模块 README
记录的是实现约束与踩过的坑，改代码前先读对应模块的 README。

当前基线是 V0.1 MVP：macOS 的 Window / Idle / Git 元数据经本地最小化后同步到 Go + SQLite 服务端，每晚调用一次 DeepSeek API 生成总结。仓库各模块 README 只记录实现约束，产品与架构决策以正式文档为准。

